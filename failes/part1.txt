度规
提交作品的评分标准是ROC曲线下的面积。

提交说明
你应该提交一个逗号分隔的文件，包含25,000行加上一行头部。应该有两列：“id”和“sentiment”，分别包含你的二元预测：1列为正面评价，0列为负面评论。举例，请参见数据页面的“sampleSubmission.csv”。

id,sentiment
123_45,0 
678_90,1
12_34,0
...

第一部分：初学者词汇袋
link
keyboard_arrow_up
什么是自然语言处理？
自然语言处理（NLP）是一套用于解决文本问题的技术。本页将帮助您开始加载和清理IMDB电影评论，然后应用一个简单的“词袋”模型，获得出人意料的准确预测，判断评论是点赞还是否认。
在你开始之前
这个教程是用Python做的。如果你以前没用过Python，我们建议你去Titanic竞赛的Python教程看看（顺便看看Random Forest的介绍）。
如果你已经熟悉Python和基础NLP技巧，可以直接跳到第二部分。
这部分教程不依赖于平台。在整个教程中，我们将使用各种Python模块进行文本处理、深度学习、随机森林及其他应用。详情请参见“设置您的系统”页面。
有很多很好的教程，甚至整本书专门讲述自然语言处理和Python文本处理。这个教程绝不是要穷尽一切——只是帮助你开始看电影评论。
代码
第一部分的教程代码就在这里。
数据解读
所需文件可从数据页面下载。你需要的第一个文件是unlabeledTrainData，包含25,000条IMDB电影评论，每条带有正面或负面情绪标签。
接下来，把制表符分隔的文件读入Python。为此，我们可以使用泰坦尼克号教程中介绍的pandas包，它提供了轻松读写数据文件的功能。如果你以前没用过Pandas，可能需要安装它。read_csv
# Import the pandas package, then use the "read_csv" function to read
# the labeled training data
import pandas as pd       
train = pd.read_csv("labeledTrainData.tsv", header=0, \
                    delimiter="\t", quoting=3)
这里，“header=0” 表示文件的第一行包含列名，“delimiter=\t” 表示字段之间用制表符分隔，quoteing=3 则让 Python 忽略重引号，否则读取文件时可能会遇到错误。
我们可以确保读取25,000行和3列，具体如下：
>>> train.shape
(25000, 3)

>>> train.columns.values
array([id, sentiment, review], dtype=object)
这三列分别称为“id”、“sentiment”和“array”。既然你已经读过训练集，不妨看看一些评测：
print train["review"][0]
提醒一下，这里会显示名为“评论”栏的第一篇电影评论。你应该看到一篇开头是这样的评论：
"With all this stuff going down at the moment with MJ i've started listening to his music, watching the odd documentary here and there, watched The Wiz and watched Moonwalker again. Maybe i just want to get a certain insight into this guy who i thought was really cool in the eighties just to maybe make up my mind whether he is guilty or innocent. Moonwalker is part biography, part feature film which i remember going to see at the cinema when it was originally released. Some of it has subtle messages about MJ's feeling towards the press and also the obvious message of drugs are bad m'kay. <br/><br/>..."
有HTML标签，比如缩写、标点符号——这些都是处理在线文本时常见的问题。顺便花点时间看看训练集里的其他综述——下一节会讲如何整理机器学习的文本。"<br/>"
数据清理与文本预处理
去除HTML标记：BeautifulSoup软件包
首先，我们会移除HTML标签。为此，我们将使用美丽汤库。如果你还没有安装Beautiful soup，请：
$ sudo pip install BeautifulSoup4
从命令行（不是在Python内部）。然后，在 Python 内部加载该包，并用它提取评论中的文本：
# Import BeautifulSoup into your workspace
from bs4 import BeautifulSoup             

# Initialize the BeautifulSoup object on a single movie review     
example1 = BeautifulSoup(train["review"][0])  

# Print the raw review and then the output of get_text(), for 
# comparison
print train["review"][0]
print example1.get_text()
打电话给你会看到评论的正文，没有标签或标记。如果你浏览BeautifulSoup的文档，你会发现这是一个非常强大的库——比我们这个数据集所需的还要强大。然而，使用正则表达式去除标记并不被认为是可靠的做法，因此即使是如此简单的应用，通常最好使用像 BeautifulSoup 这样的包。get_text()
处理标点符号、数字与停止词：NLTK与正则表达式
在考虑如何清理文本时，我们应考虑我们试图解决的数据问题。对于许多问题，去除标点符号是合理的。另一方面，在这种情况下，我们正在处理情感分析问题，“!!”或“：-（”）可能带有情感，应当作为词语来处理。在这个教程中，为了简化起见，我们完全去掉了标点符号，但你可以自己尝试。
同样，在这个教程中我们会去掉数字，但还有其他处理它们的方法同样合理。例如，我们可以将它们视为单词，或者全部替换为占位字符串如“NUM”。
为了去除标点符号和数字，我们将使用一个处理正则表达式的包，称为 Python 内置的包;不需要安装任何东西。关于正则表达式的工作原理，请参见包文档。现在，试试以下方法：re.
import re
# Use regular expressions to do a find-and-replace
letters_only = re.sub("[^a-zA-Z]",           # The pattern to search for
                      " ",                   # The pattern to replace it with
                      example1.get_text() )  # The text to search
print letters_only
正则表达式的完整概述超出本教程范围，但目前只需知道 表示群成员身份 和 表示“非”即可。换句话说，上面的 re.sub（） 语句是：“找任何不是小写字母（）或大写字母（）的东西，然后用空格替换。”[]^a-zA-Z
我们还会将评论转换为小写，并拆分成单个词（在自然语言处理术语中称为“代币化”）：
lower_case = letters_only.lower()        # Convert to lower case
words = lower_case.split()               # Split into words
最后，我们需要决定如何处理那些经常出现但意义不大的词。这些词被称为“停止词”;在英语中，它们包含了诸如“a”、“and”、“is”和“the”这样的词。方便的是，有些 Python 包内置了停止词列表。让我们从Python自然语言工具包（NLTK）导入一个停止词列表。如果你电脑上还没有安装库，你需要安装它;你还需要安装随它附带的数据包，具体步骤如下：
import nltk
nltk.download()  # Download text data sets, including stop words
现在我们可以用nltk获得一个停止词列表：
from nltk.corpus import stopwords # Import the stop word list
print stopwords.words("english") 
这样你就能查看英语的停用词列表。要去除我们电影评论中的停顿词，请：
# Remove stop words from "words"
words = [w for w in words if not w in stopwords.words("english")]
print words
它会查看“单词”列表中的每个单词，并剔除该词列表中出现的任何单词。经过所有这些步骤，你的评审现在应该会这样开始：
[u'stuff', u'going', u'moment', u'mj', u've', u'started', u'listening', u'music', u'watching', u'odd', u'documentary', u'watched', u'wiz', u'watched', u'moonwalker', u'maybe', u'want', u'get', u'certain', u'insight', u'guy', u'thought', u'really', u'cool', u'eighties', u'maybe', u'make', u'mind', u'whether', u'guilty', u'innocent', u'moonwalker', u'part', u'biography', u'part', u'feature', u'film', u'remember', u'going', u'see', u'cinema', u'originally', u'released', u'subtle', u'messages', u'mj', u'feeling', u'towards', u'press', u'also', u'obvious', u'message', u'drugs', u'bad', u'm', u'kay',.....]
别担心每个词前面的“u”;它只是表明 Python 内部将每个单词表示为 Unicode 字符串。
我们还有很多其他方法可以对数据做——例如，Porter Stemming 和 Lemmatizing （NLTK 中都有）可以让我们把“messages”、“message”和“messaging”当作同一个词来处理，这确实很有用。不过，为了简化起见，教程就到这里。
把一切整合起来
现在我们有代码清理一条评审——但我们需要清理25,000条培训评审！为了让代码可复用，我们创建一个可以多次调用的函数：
def review_to_words( raw_review ):
    # Function to convert a raw review to a string of words
    # The input is a single string (a raw movie review), and 
    # the output is a single string (a preprocessed movie review)
    #
    # 1. Remove HTML
    review_text = BeautifulSoup(raw_review).get_text() 
    #
    # 2. Remove non-letters        
    letters_only = re.sub("[^a-zA-Z]", " ", review_text) 
    #
    # 3. Convert to lower case, split into individual words
    words = letters_only.lower().split()                             
    #
    # 4. In Python, searching a set is much faster than searching
    #   a list, so convert the stop words to a set
    stops = set(stopwords.words("english"))                  
    # 
    # 5. Remove stop words
    meaningful_words = [w for w in words if not w in stops]   
    #
    # 6. Join the words back into one string separated by space, 
    # and return the result.
    return( " ".join( meaningful_words ))   
这里有两个新元素：首先，我们将停止词列表转换为不同的数据类型，即集合。这是为了速度;由于我们将调用该函数数万次，它需要快速，而在 Python 中搜索集合比搜索列表快得多。
其次，我们把这些词汇合并回一个段落。这是为了让输出内容在下面的“词语袋”中更方便使用。定义上述函数后，如果你调用该函数进行单次评审：
clean_review = review_to_words( train["review"][0] )
print clean_review
它应该会给你和之前教程中所有步骤的完全相同的结果。现在让我们一次清理所有训练集（这可能需要几分钟，具体取决于你的电脑）：
# Get the number of reviews based on the dataframe column size
num_reviews = train["review"].size

# Initialize an empty list to hold the clean reviews
clean_train_reviews = []

# Loop over each review; create an index i that goes from 0 to the length
# of the movie review list 
for i in xrange( 0, num_reviews ):
    # Call our function for each one, and add the result to the list of
    # clean reviews
    clean_train_reviews.append( review_to_words( train["review"][i] ) )
有时候等待一段冗长的代码运行会让人感到烦躁。写代码以更新状态会很有帮助。要让 Python 在处理每 1000 条审核后打印状态更新，试着在上面的代码里加一两行：
print "Cleaning and parsing the training set movie reviews...\n"
clean_train_reviews = []
for i in xrange( 0, num_reviews ):
    # If the index is evenly divisible by 1000, print a message
    if( (i+1)%1000 == 0 ):
        print "Review %d of %d\n" % ( i+1, num_reviews )                                                                    
    clean_train_reviews.append( review_to_words( train["review"][i] ))
从单词袋中创建特征（使用 scikit-learn）
既然我们的训练评审已经整理好，我们如何将它们转化为机器学习的某种数值表示？一种常见的方法叫做“词袋”。词袋模型从所有文档中学习词汇，然后通过计算每个单词出现的次数来建模每份文档。例如，考虑以下两句话：
句子一：“猫坐在帽子上”
句子二：“狗吃了猫和帽子”
从这两句话中，我们的词汇如下：
{ the， cat， sat， on， hat， dog， ate and }
为了获得我们的词袋，我们会统计每个词在每句话中出现的次数。在第一句中，“the”出现两次，“cat”、“sat”、“on”和“hat”各出现一次，因此第一句的特征向量为：
{ the， cat， sat， on， hat， dog， ate and }
句子1：{ 2， 1， 1， 1， 1， 0， 0， 0 }
同样，句子2的特征为：{3， 1， 0， 0， 1， 1， 1， 1}
在IMDB数据中，我们有大量评论，这会让我们拥有丰富的词汇量。为了限制特征向量的大小，我们应选择一个最大词汇量。下面，我们使用了5000个最常用的词（记住，停止词已经被移除）。
我们将使用scikit-learn的feature_extraction模块来创建单词包功能。如果你在泰坦尼克号比赛中做过随机森林教程，应该已经安装了scikit-learn;否则你就得自己安装。
print "Creating the bag of words...\n"
from sklearn.feature_extraction.text import CountVectorizer

# Initialize the "CountVectorizer" object, which is scikit-learn's
# bag of words tool.  
vectorizer = CountVectorizer(analyzer = "word",   \
                             tokenizer = None,    \
                             preprocessor = None, \
                             stop_words = None,   \
                             max_features = 5000) 

# fit_transform() does two functions: First, it fits the model
# and learns the vocabulary; second, it transforms our training data
# into feature vectors. The input to fit_transform should be a list of 
# strings.
train_data_features = vectorizer.fit_transform(clean_train_reviews)

# Numpy arrays are easy to work with, so convert the result to an 
# array
train_data_features = train_data_features.toarray()
要查看训练数据数组现在的样子，请执行：
>>> print train_data_features.shape
(25000, 5000)
它有25,000行和5,000个特征（每个词汇一个）。
注意，CountVectorizer 自带自动预处理、分词和停止字移除的选项——对于这些选项，我们本可以用内置方法或自定义函数来使用，而不是选择“无”。更多细节请参见函数文档。不过，我们想在本教程中写自己的数据清理函数，一步步向你展示如何完成。
现在词袋模型已经训练好，让我们来看词汇：
# Take a look at the words in the vocabulary
vocab = vectorizer.get_feature_names()
print vocab
如果你感兴趣，也可以打印词汇表
中每个单词的计数：
import numpy as np

# Sum up the counts of each vocabulary word
dist = np.sum(train_data_features, axis=0)

# For each, print the vocabulary word and the number of times it 
# appears in the training set
for tag, count in zip(vocab, dist):
    print count, tag
随机森林
此时，我们已经有了来自词袋的数值训练特征和每个特征向量的原始情感标签，所以让我们进行一些监督学习吧！这里，我们将使用我们在泰坦尼克号教程中介绍的随机森林分类器。随机森林算法被包含在 scikit-learn（随机森林使用许多基于树的分类器进行预测，因此称为“森林”）。下面，我们将树的数量设为100，作为合理的默认值。更多的树可能表现更好（也可能不行），但生长时间肯定会更长。同样，每篇评测中包含的功能越多，完成过程就越长。
print "Training the random forest..."
from sklearn.ensemble import RandomForestClassifier

# Initialize a Random Forest classifier with 100 trees
forest = RandomForestClassifier(n_estimators = 100) 

# Fit the forest to the training set, using the bag of words as 
# features and the sentiment labels as the response variable
#
# This may take a few minutes to run
forest = forest.fit( train_data_features, train["sentiment"] )
创建提交
剩下的就是在我们的测试集中运行训练好的随机森林并创建一个提交文件。如果你还没下载，可以从数据页面下载testData.tsv。该文件包含另外25,000条评论和ID;我们的任务是预测情感标签。
注意，当我们用词袋来做测试集时，只调用“transform”，而不是像训练集那样叫“fit_transform”。在机器学习中，你不应该用测试集来拟合模型，否则有过拟合的风险。因此，我们会在准备好做出预测之前，禁止使用测试集。
# Read the test data
test = pd.read_csv("testData.tsv", header=0, delimiter="\t", \
                   quoting=3 )

# Verify that there are 25,000 rows and 2 columns
print test.shape

# Create an empty list and append the clean reviews one by one
num_reviews = len(test["review"])
clean_test_reviews = [] 

print "Cleaning and parsing the test set movie reviews...\n"
for i in xrange(0,num_reviews):
    if( (i+1) % 1000 == 0 ):
        print "Review %d of %d\n" % (i+1, num_reviews)
    clean_review = review_to_words( test["review"][i] )
    clean_test_reviews.append( clean_review )

# Get a bag of words for the test set, and convert to a numpy array
test_data_features = vectorizer.transform(clean_test_reviews)
test_data_features = test_data_features.toarray()

# Use the random forest to make sentiment label predictions
result = forest.predict(test_data_features)

# Copy the results to a pandas dataframe with an "id" column and
# a "sentiment" column
output = pd.DataFrame( data={"id":test["id"], "sentiment":result} )

# Use pandas to write the comma-separated output file
output.to_csv( "Bag_of_Words_model.csv", index=False, quoting=3 )
恭喜你，你已经准备好提交你的第一篇作品了！尝试不同方法，看看效果如何变化。你可以用不同的方式清理评论，为“词袋”表示选择不同数量的词汇，尝试Porter词干，换个分类器，或者做其他很多方法。想在不同的数据集上试试你的自然语言处理技巧，也可以去我们的烂番茄竞赛。或者，如果你准备好尝试完全不同的内容，可以继续浏览深度学习和Word Vector页面。